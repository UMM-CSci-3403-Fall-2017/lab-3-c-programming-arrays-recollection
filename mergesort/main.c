#include "mergesort.h"
#include <stdlib.h>
#include <stdio.h>

int main() {
    int i;
    int arr[]= {12, 13, 11, 6, 7, 5};
    int arrSize = sizeof(arr)/sizeof(arr[0]);
    
    mergeSort(arrySize, arry)
    
    for (i=0; i< arrSize; i++){
        printf("%d", arr[i]);
        printf("/n");
    }
}
